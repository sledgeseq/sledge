# Build artifacts and local binaries
/bin/
/build/
/dev/
/external_tools/
/results/
/build-config.mk
# Test run logs and artifacts (see install/test_installation.sh)
/install/info/

# Local helper; not intended for the remote (use for bulk commits/pushes)
/combo_commit.sh
